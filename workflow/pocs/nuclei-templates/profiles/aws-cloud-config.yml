# Nuclei scan profile for scanning aws ACLs

code: true # enable code templates

tags:
  - aws-cloud-workflowstruct # filter templates with "aws-cloud-workflowstruct" tags

var:
  - region=us-east-1 # template input for "region" variable